let PageDir = Request.ServerVariables.URL;
Debug.LogInfo("PageDir: " + PageDir);

const RedirectMap = function(Key) {
	return {
		"/" : "views/nav.esp",
		"/admin" : "views/admin.esp",
		"/css/all" : "views/css.esp",
		"/js/all" : "views/js.esp"
	}[Key];
}

Debug.LogInfo("Redirecting to : " + RedirectMap(PageDir));
Server.Transfer(RedirectMap(PageDir));

// Server.Transfer('views/nav.esp')